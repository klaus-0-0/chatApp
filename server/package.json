{
    "scripts": {
    "start": "node main/index.js",
    "dev": "nodemon main/index.js",
    "generate": "prisma generate",
    "migrate": "prisma migrate deploy",
    "setup": "npm run generate && npm run migrate && npm run start"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "nodemon": "^3.1.9",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "prisma": "^6.5.0"
  }
}
